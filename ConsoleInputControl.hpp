#ifndef ConsoleInputControl_hpp
#define ConsoleInputControl_hpp

#include <string>

using namespace std; 

int checkIntegerInput(string s);
float checkFloatInput(string s);
char checkCharacterInput(string s);

#endif
